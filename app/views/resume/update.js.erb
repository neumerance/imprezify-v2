updateContent('#preview', '<%= escape_javascript(render partial: 'preview', locals: { resume: @resume }) %>')
updateContent('#form-navigation', '<%= render partial: 'resume/form_navigation', locals: { resume: @resume } %>');
window.initStickyBits();
